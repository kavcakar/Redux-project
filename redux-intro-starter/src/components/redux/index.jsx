
const initialState = {
     counter: 0
}

export const reducer = {state = initialState, action} => {
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}
